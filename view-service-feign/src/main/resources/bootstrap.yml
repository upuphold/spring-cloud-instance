spring:
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled:  true
        serviceId:  CONFIG-SERVER
  client:
    serviceUrl:
      defaultZone:  http://localhost:9000/eureka/