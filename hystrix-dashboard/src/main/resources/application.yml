spring:
  application:
    name: hystrix-dashboard
server:
  port: 9020

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/
    fetch-registry: true
    register-with-eureka: true
