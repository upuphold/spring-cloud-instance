spring:
  application:
    name: config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/upuphold/config
          search-paths: respo
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/
server:
  port: 9012